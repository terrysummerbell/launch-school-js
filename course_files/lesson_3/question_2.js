// question 2

// my solution
let str1 = "Come over here!"; // true
let str2 = "What's up, Doc!"; // false

str1[str1.length - 1].includes("!");
str2[str2.length - 1].includes("!");

// Launch School solutiom

str1.endsWith("!");
str2.endsWith("!");